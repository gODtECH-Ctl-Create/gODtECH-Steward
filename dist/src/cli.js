#!/usr/bin/env node
import { writeFile } from "node:fs/promises";
import { resolve } from "node:path";
import { collectFiles } from "./core/files.js";
import { applySafeFixes } from "./rules/hygiene.js";
import { initConfig, loadConfig } from "./core/config.js";
import { scanRepository } from "./core/scanner.js";
import { hasCiFailure, toJson, toText } from "./core/report.js";
function usage() { return `gODtECH Steward\n\nUsage:\n  steward init [path]\n  steward scan [path] [--json] [--ci]\n  steward doctor [path] [--json] [--ci]\n  steward report [path] --output <file>\n  steward fix [path] --safe\n  steward --version\n`; }
function option(args, name) { const index = args.indexOf(name); return index >= 0 ? args[index + 1] : undefined; }
function targetPath(args) { for (let i = 1; i < args.length; i += 1) {\n    const arg = args[i];\n    if (arg === "--output") {\n        i += 1;\n        continue;\n    }\n    if (!arg.startsWith("--") && arg !== "scan" && arg !== "doctor" && arg !== "report" && arg !== "fix" && arg !== "init")\n        return arg;\n} return "."; }
async function main() { const args = process.argv.slice(2); const command = args[0] ?? "help"; if (command === "--version" || command === "-v") {\n    console.log("0.1.0");\n    return;\n} if (command === "help" || command === "--help" || command === "-h") {\n    console.log(usage());\n    return;\n} const root = resolve(targetPath(args)); if (command === "init") {\n    const created = await initConfig(root);\n    console.log(created ? "Created .steward.json" : ".steward.json already exists");\n    return;\n} if (command === "fix") {\n    if (!args.includes("--safe"))\n        throw new Error("Refusing to modify files without explicit --safe.");\n    const config = await loadConfig(root);\n    const files = await collectFiles(root, config);\n    const changed = applySafeFixes(files);\n    console.log(changed.length ? `Safe fixes applied to ${changed.length} file(s):\n${changed.map(file => `- ${file}`).join("\n")}` : "No safe fixes were necessary.");\n    return;\n} if (!["scan", "doctor", "report"].includes(command))\n    throw new Error(`Unknown command: ${command}\n\n${usage()}`); const result = await scanRepository(root); const config = await loadConfig(root); if (command === "report") {\n    const output = option(args, "--output");\n    if (!output)\n        throw new Error("report requires --output <file>");\n    await writeFile(resolve(root, output), toJson(result) + "\n", "utf8");\n    console.log(`Report written to ${output}`);\n}\nelse if (args.includes("--json"))\n    console.log(toJson(result));\nelse\n    console.log(toText(result)); if (args.includes("--ci") && hasCiFailure(result, config.ci.failOn))\n    process.exitCode = 1; }\nmain().catch((error) => { console.error(error instanceof Error ? error.message : String(error)); process.exitCode = 2; });
