import { performance } from "node:perf_hooks";
import { relative, resolve, sep } from "node:path";
import { collectFiles, normalisePath } from "./files.js";
import { loadConfig } from "./config.js";
import { gitFacts } from "./git.js";
import { enabledPackSummaries, enabledRules } from "./rules.js";
import { scoreFindings } from "./score.js";
import { findingFingerprint } from "./delta.js";
function categoryCounts(findings) { return findings.reduce((counts, finding) => { counts[finding.category] = (counts[finding.category] ?? 0) + 1; return counts; }, {}); }
function ruleCounts(findings) { return findings.reduce((counts, finding) => { counts[finding.rule] = (counts[finding.rule] ?? 0) + 1; return counts; }, {}); }
function scopedTrackedFiles(root, gitRoot, tracked) { if (!gitRoot || resolve(root) === resolve(gitRoot)) return new Set(tracked); const scoped = new Set(); for (const path of tracked) { const absolute = resolve(gitRoot, path); const relativePath = relative(root, absolute); const inside = relativePath === "" || (relativePath !== ".." && !relativePath.startsWith(`..${sep}`)); if (inside) scoped.add(normalisePath(relativePath)); } return scoped; }
function configFinding(message) { return { id: "configuration.invalid-steward-config", rule: "invalid-steward-config", category: "configuration", severity: "critical", message: `Invalid .steward.json: ${message}`, path: ".steward.json", fixable: false, confidence: "high", remediation: "Repair .steward.json or remove it to return to the built-in defaults." }; }
function withFingerprints(findings) { return findings.map((finding) => ({ ...finding, fingerprint: findingFingerprint(finding) })); }
export async function scanRepository(inputRoot) { const start = performance.now(); const root = resolve(inputRoot); const loaded = await loadConfig(root); const config = loaded.config; const git = gitFacts(root); const files = await collectFiles(root, config); const scopedTracked = scopedTrackedFiles(root, git.root, git.trackedFiles); const context = { root, config, files, trackedFiles: scopedTracked, git: { ...git, trackedFiles: scopedTracked } }; const enabledPacks = enabledPackSummaries(config.packs.disabled); const findings = []; if (loaded.warning) findings.push(configFinding(loaded.warning)); for (const rule of enabledRules(config.rules.disabled, config.packs.disabled)) { try { findings.push(...rule.run(context)); } catch (error) { findings.push({ id: `internal.rule-failure.${rule.id}`, rule: "rule-execution-failure", category: "configuration", severity: "critical", message: `Rule ${rule.id} failed during scanning.`, fixable: false, confidence: "high", details: error instanceof Error ? error.message : "Unknown rule error.", remediation: "Investigate the rule failure before relying on the scan as complete." }); } } const fingerprinted = withFingerprints(findings); fingerprinted.sort((a, b) => (SEVERITY_ORDER.get(a.severity) ?? 99) - (SEVERITY_ORDER.get(b.severity) ?? 99) || (a.path ?? "").localeCompare(b.path ?? "") || a.id.localeCompare(b.id)); return { version: 1, root, scannedFiles: files.length, textFiles: files.filter((file) => file.isText).length, findings: fingerprinted, healthScore: scoreFindings(fingerprinted), durationMs: Math.max(0, Math.round(performance.now() - start)), git: context.git, categoryCounts: categoryCounts(fingerprinted), ruleCounts: ruleCounts(fingerprinted), rulePacks: enabledPacks }; }
